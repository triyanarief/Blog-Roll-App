# Blog-Roll-App
Make blog roll app with backboneJs

## Start Setup

``` bash
# install dependencies
npm install

# Running server mongoDb at localhost
mongod

# then start database
mongo

# start the app
node server
```
